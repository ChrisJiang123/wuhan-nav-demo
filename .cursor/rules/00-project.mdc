# Cursor 规则 — 项目总则

> 这是 Cursor 在本仓库的常驻规则（always-on）。与根目录 `AGENTS.md` 一致；冲突时以 `AGENTS.md` 的“锁定口径”和“绝对禁止”为准。

## 角色分工

- **Cursor（你）**：主 IDE 内开发流——脚手架、页面骨架、状态管理、样式、交互联调、随手重构。
- **Codex**：并行子任务——API client / DTO 生成、测试样例、文档初稿、PR 级审查、云端长任务。

不要重复 Codex 已在做的并行任务；专注于人在 IDE 里实时驱动的主线开发。

## 项目锁定口径（务必遵守）

- 开源自搭：OSM + MapLibre + 自建路由（OSRM 默认）。**禁止商业地图 SDK/API。**
- 仅 Android。不写 iOS 代码。
- 全链路 WGS-84，**不做坐标转换**。
- 实时路况/事件本周不做真实数据源。
- 前端一切外部数据经 BFF，不直连路由引擎/数据库。

## 代码规范

- 前端 Flutter：feature-first 目录（`features/<name>/`），共享逻辑入 `core/`。
- 状态管理统一一种（默认 Riverpod 或 Provider，首个相关任务里定下后不再换）。
- 后端 NestJS：模块化（map-bff / report-service），DTO 放 `packages/shared-types` 并前后端复用。
- 命名：文件 snake_case（dart）/ kebab-case（ts 文件）；类型 PascalCase；变量 camelCase。
- 任何密钥走环境变量，参考 `.env.example`，禁止硬编码。

## UI / 交互规范

- 设计 token 见下方“视觉”小节，颜色只服务路况与路线，不服务营销。
- 导航主界面一级控件 ≤ 5 个。
- 上报动作驾驶态 2 步内完成。
- 不做开屏广告、不做与导航无关的弹窗。
- 动效短促低弹性（180–240ms），导航态底卡纵向滑出。

## 视觉 token

| 语义 | 色值 |
|---|---|
| 主品牌 / 当前路线 / 主按钮 | #1570EF |
| 强调 / 开始导航 / 成功 / 已上报 | #12B76A |
| 警告 / 中度拥堵 / 施工 | #F79009 |
| 危险 / 重度拥堵 / 事故 / 错误 | #D92D20 |
| 白天底色 | #F8FAFC |
| 夜间底色 / 底部面板 | #0F172A |
| 次级 / 辅助文字 | #475467 / #98A2B3 |

## 提交前

- `flutter analyze` 与后端 `npm run lint` 通过。
- 不引入未在 `docs/TASKS.md` 列出的功能。
- 模糊处先问，不自行假设。
