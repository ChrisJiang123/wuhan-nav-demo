{
  "name": "@wuhan-nav/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Mock and fixture data for the Wuhan navigation demo.",
  "type": "module",
  "exports": {
    "./wuhan-pois.json": "./wuhan-pois.json",
    "./wuhan-routes.json": "./wuhan-routes.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "mock:bff": "node mock-bff.mjs",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@wuhan-nav/shared-types": "file:../shared-types"
  }
}
