name: test_fixtures
description: Flutter asset bundle for @wuhan-nav/test-fixtures JSON files.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.7.0 <4.0.0"

flutter:
  assets:
    - assets/wuhan-pois.json
    - assets/wuhan-routes.json
